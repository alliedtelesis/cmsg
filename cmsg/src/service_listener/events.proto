/*
 * Copyright 2019, Allied Telesis Labs New Zealand, Ltd
 */

package cmsg_sld;

import "cmsg.proto";

service events
{
    rpc server_added (cmsg_service_info) returns (dummy);
    rpc server_removed (cmsg_service_info) returns (dummy);
}